<odoo>
    <data noupdate="1">
        <record id="ir_cron_adjust_work_entry_time" model="ir.cron">
            <field name="name">Deviation: Adjust Work Entry Time</field>
            <field name="model_id" ref="model_hr_attendance"/>
            <field name="state">code</field>
            <field name="code">model.cron_adjust_work_entry_time()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(DateTime.now().replace(hour=3, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')" />
            <field name="active">True</field>
        </record>

        <record id="ir_cron_correct_invalid_hr_attendance" model="ir.cron">
            <field name="name">Deviation: Correct Invalid HR Attendance</field>
            <field name="model_id" ref="model_hr_attendance"/>
            <field name="state">code</field>
            <field name="code">model.cron_correct_invalid_attendance()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(DateTime.now().replace(hour=4, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')" />
            <field name="active">True</field>
        </record>
    </data>
</odoo>